[project]
name = "test-bids"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "nibabel",
    "antspyx",
    "numpy",
]

[project.scripts]
anat-preproc = "preprocess:main"
extract-structurals = "extract_bids:main"
